#include <bits/stdc++.h>

using namespace std;

vector<long long>V;
long long a;
int n,k;

int main() {
	cin >> n >> k;
	for (int i = 1;i<=n;i++){
		cin >> a;
		V.push_back(a);
	}
	sort(V.begin(), V.end());
	cout << V[k-1] << endl;
}
